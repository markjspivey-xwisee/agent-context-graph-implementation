# Databricks JDBC Driver (Local Install)

Download the Databricks **JDBC driver** for Linux and place the `.jar` in this folder.

Suggested location:

```
drivers/databricks-jdbc/DatabricksJDBC42.jar
```

Notes:
- The JDBC driver is required by Ontop to connect to Databricks.
- This repo does **not** commit the driver binary.
